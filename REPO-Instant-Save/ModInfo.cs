namespace REPO_Instant_Save
{
    /// <summary>
    /// Static identity constants for the plugin. Kept in one place so the
    /// <see cref="BepInEx.BepInPlugin"/> attribute, config sections, and save
    /// folder names all stay in sync.
    /// </summary>
    internal static class ModInfo
    {
        public const string Guid = "Ashx87.REPOInstantSave";
        public const string Name = "REPO Instant Save";
        public const string Version = "0.1.0";
    }
}
